services:
  paper-indexer:
    build: .
    container_name: paper-indexer
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PAPER_PORT:-8089}:${PAPER_PORT:-8089}"
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
      - ./config:/app/config
